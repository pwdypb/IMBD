version: 2
jobs:
   build:
     docker:
       - image: circleci/openjdk:8-jdk-browsers
     steps:
       - checkout
       - run: echo "hello world"
       - run: mvn dependency:go-offline
       - run: mvn package
